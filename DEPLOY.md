# העלאה ל-Render

האתר הזה הוא **אתר סטטי**: Vite בונה אותו ל-`dist/`, ואין לו שרת משלו.
זה קובע את כל השאר.

---

## הטעות שכדאי לא לחזור עליה

ב-Render יש שני סוגי שירות שנראים דומים, והבחירה ביניהם היא ההבדל בין אתר
שעובד לאתר שמחזיר 503:

| | **Static Site** ← הנכון כאן | **Web Service** |
|---|---|---|
| למה הוא מיועד | קבצים בנויים | תהליך שרץ ומאזין לפורט |
| בתוכנית החינמית | **תמיד למעלה** | **נכבה אחרי חוסר פעילות** |
| התעוררות | אין צורך | 30–60 שניות, ולפעמים 503 |
| עלות | חינם | חינם עם השינה הזו |

השירות הקודם החזיר `503` בשלוש בדיקות רצופות של 45 שניות. התעוררות קרה מחזירה
200 באיחור, לא 503 — כלומר הוא לא ישן, הוא היה כבוי או תקול. בכל מקרה: אתר
סטטי בתוכנית החינמית **לא נכבה בכלל**, ולכן הבעיה הזו נעלמת מעצמה.

---

## ההגדרות המדויקות

**New → Static Site**, מחברים את `Galomer310/Portfolio-2025`:

| שדה | ערך |
|---|---|
| Branch | `main` |
| Root Directory | *(ריק)* |
| Build Command | `npm ci && npm run build` |
| Publish Directory | `dist` |
| Auto-Deploy | On |

אין Start Command ואין פורט — אלה שייכים ל-Web Service.

---

## משתני הסביבה — הנקודה שתופסת אנשים

הטופס משתמש ב-EmailJS ומצפה לשלושה משתנים. **ב-Vite הם נצרבים לתוך ה-JS בזמן
הבנייה**, לא נקראים בזמן ריצה. המשמעות:

> אם תוסיף אותם ב-Render אחרי שהבנייה כבר רצה — הם לא יופיעו באתר
> עד שתריץ **Manual Deploy → Clear build cache & deploy**.

תחת **Environment**:

```
VITE_EMAILJS_SERVICE_ID    = service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID   = template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY    = xxxxxxxxxxxxxxx
```

את שלושתם לוקחים מ-[dashboard.emailjs.com](https://dashboard.emailjs.com) —
Service ID מ-Email Services, Template ID מ-Email Templates, Public Key מ-Account.

`VITE_EMAILJS_USER_ID` מוצהר ב-`vite-env.d.ts` אבל **לא בשימוש** — אפשר להתעלם.

**המפתח הציבורי באמת ציבורי.** הוא יופיע בקוד ה-JS שהדפדפן מוריד, וזה בסדר —
כך EmailJS בנוי. ההגנה היא ברשימת הדומיינים המורשים בלוח של EmailJS, לא
בהסתרת המפתח. **כדאי להגדיר שם את הדומיין של Render** אחרי הפריסה הראשונה,
אחרת כל אחד יכול לשלוח דרך החשבון שלך.

---

## כלל Rewrite — ביטוח זול

הניווט באתר הוא עוגנים (`#home`, `#about`) על עמוד אחד, ולכן **כרגע** אין צורך
בזה. אבל `main.tsx` עוטף את האפליקציה ב-`BrowserRouter`, וברגע שיתווסף נתיב
אמיתי — כניסה ישירה אליו תחזיר 404 מ-Render.

תחת **Redirects / Rewrites**, כדי לסגור את זה מראש:

| Source | Destination | Action |
|---|---|---|
| `/*` | `/index.html` | Rewrite |

חשוב ש-Action יהיה **Rewrite** ולא Redirect: Rewrite מגיש את `index.html`
והכתובת בסרגל נשארת, Redirect ישלח את המשתמש ל-`/index.html` ויראה לו את זה.

---

## בדיקה אחרי פריסה

1. הדף נטען, והקונסול נקי
2. **הטופס** — שלח לעצמך הודעת בדיקה. זו הדרך היחידה לדעת שהמשתנים נצרבו
3. **בטלפון** — זה המקרה האמיתי, לא הדסקטופ
4. **הקישורים לפרויקטים** — חלק מהדמואים יושבים על Render חינמי בעצמם ולכן
   ישנים. קישור שלוקח 40 שניות נראה שבור. שווה לסמן אותם, או להעביר את הדמואים
   החשובים לאירוח סטטי

---

## אחרי שזה עובד

- **דומיין משלך** תחת Settings → Custom Domain. `galomer.dev` נראה אחרת
  מ-`portfolio-2025-m45j.onrender.com` בקורות חיים, והתעודה חינם ואוטומטית
- אם השארת את השירות הישן — **מחק אותו**, כדי שלא יהיו שתי כתובות חיות

---

## למה אין כאן `render.yaml`

אפשר, אבל לאתר סטטי יחיד זה קובץ שצריך לתחזק בשביל חמישה שדות שמוגדרים פעם
אחת. `shieldProject` כן מצדיק אחד — יש בו שרת, מסד נתונים וסודות. כאן לא.
